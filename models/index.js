const reaction = require('./Reaction');
const Thoughts = require('./Thoughts');
const User = require('./Users');

// exporting the thoughts and user models 
module.exports = { reaction, Thoughts, User };